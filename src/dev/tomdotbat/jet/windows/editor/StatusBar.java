package dev.tomdotbat.jet.windows.editor;

import dev.tomdotbat.jet.windows.EditorWindow;

public class StatusBar {
    public StatusBar(EditorWindow window) {
        this.window = window;
    }

    private final EditorWindow window;
}
